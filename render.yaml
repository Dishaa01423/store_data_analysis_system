services:
  - type: web
    name: store_data_analysis
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "10000"
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
